app:
  scheduler:
    interval-hours: ${SCHEDULER_INTERVAL_HOURS:1}
  training-peaks:
    api-url: https://tpapi.trainingpeaks.com
  intervals:
    api-url: https://intervals.icu
  trainer-road:
    api-url: https://www.trainerroad.com
  attachments:
    enabled: false

spring:
  cloud:
    openfeign:
      client:
        config:
          default:
            loggerLevel: basic
  jackson:
    default-property-inclusion: non_null
    serialization:
      fail-on-empty-beans: false
  datasource:
    url: jdbc:sqlite:/data/tp2intervals.sqlite
    driver-class-name: org.sqlite.JDBC
  jpa:
    open-in-view: false
    database-platform: org.hibernate.community.dialect.SQLiteDialect
  cache:
    jcache:
      config: classpath:ehcache.xml

server:
  error:
    include-message: never

management:
  info:
    git:
      mode: full
  endpoints:
    web:
      exposure:
        include: health,info
      cors:
        allowed-origins: '*'
  endpoint:
    loggers:
      enabled: false

logging:
  file:
    name: /data/tp2intervals.log
